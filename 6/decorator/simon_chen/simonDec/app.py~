from flask import Flask, render_template,session
import auth

app = Flask(__name__)

@app.route("/")
@check
def index():
    return "Hello"

if __name__ == "__main__":
    app.debug = True
    app.run(host='0.0.0.0',port=5000)
   
